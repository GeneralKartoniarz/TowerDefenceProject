#pragma once
#include "Tower.h"

 //@class MODTEMPLATE
 //@brief Stworz wlasna wiezyczke na wlasne potrzeby walki z EJAJ
 //W klasie gamestate nie zapomnij zaimportowac tej klasy na samej gorze pliku!!

class MODTEMPLATE : public Tower
{
public:
    // Inicjalizuje wie¿ê na okreœlonych wspó³rzêdnych mapy
    // MODTEMPLATE(sf::Vector2f position);
    //string path = "MODTEMPLATE"; // sciezka do grafiki!
    // Sta³y koszt zakupu jednostki wykorzystywany przez system ekonomii gry
    //static constexpr int COST = MODTEMPLATE;
    // Jezeli wieza ma strzelac dodaj vector z Bulletami
    //void Update(float dt, vector<unique_ptr<Monster>>& monsters, vector<unique_ptr<Bullet>>& bullets) override;

    // Odpowiada za wyœwietlenie grafiki wie¿y oraz opcjonalnych elema entów (np. zasiêgu)
    //void Draw(sf::RenderWindow& window) override;
    //sf::SoundBuffer shootBuffer;
    //sf::Sound shootSound;
private:

};

